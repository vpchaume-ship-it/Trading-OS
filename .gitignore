__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Secrets — ne JAMAIS committer
.env

# Données de marché et contenus personnels
data/*.csv
data/*.parquet
# …sauf les données Yahoo accumulées par la routine quotidienne (persistance
# entre sessions cloud : c'est la base du backtest évolutif du dashboard)
!data/yahoo_*.csv
# état du réglage automatique de stratégie (choisi chaque matin par la routine)
!data/strategy_state.json
# NB : les PDF de knowledge/ SONT versionnés (dépôt privé) pour survivre aux
# sessions — retirez-les du dépôt si vous ne le souhaitez pas.
journal/backtests/
journal/reports/
journal/news/*
# l'historique des red folders est versionné (étiquetage NTZ du backtest)
!journal/news/history.csv
journal/trades.csv
journal/forward_state.json
knowledge/index.json
